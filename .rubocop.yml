plugins:
  - rubocop-rake

inherit_gem:
  rubocop-inst:
  - rubocop.yml
  - rubocop-rspec.yml

AllCops:
  TargetRubyVersion: 3.2

Style/Documentation:
  Enabled: false

Naming/FileName:
  Exclude:
    - lib/ruby-saml2-sans.rb # matches the gem name for Bundler.require
